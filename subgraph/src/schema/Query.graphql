extend schema @link(url: "https://specs.apollo.dev/federation/v2.6", import: ["@key"])

scalar JSON
scalar JSONObject

type Query {
  patient(where: JSONObject): Patient
}

type Patient {
  patientId: ID!
  firstName: String
  lastName: String
}
